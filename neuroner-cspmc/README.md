# neuroner-cspmc

NeuroNER LSTM-CRF engine (Python 3.7 / TensorFlow 1.x).

This directory contains only the `neuroner` Python package and its virtualenv. All data, models, and output live at the project root. See [docs/neuroner-setup.md](../docs/neuroner-setup.md) for setup and usage.
